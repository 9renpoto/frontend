machine:
  node:
    version: 7.10.0

dependencies:
  pre:
    - git config --global user.name "CircleCI"
    - git config --global user.email "9renpoto@gmail.com"

deployment:
  master:
    branch: master
    commands:
      - npm run deploy
