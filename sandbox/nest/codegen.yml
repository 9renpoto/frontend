schema: http://localhost:3000/graphql
generates:
  ./src/graphqlTypes.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-resolvers
      - typescript-react-apollo
    config:
      withComponent: false
      withHooks: true
