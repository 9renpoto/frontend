{
  "name": "@9renpoto/slides",
  "private": true,
  "scripts": {
    "build": "gulp build",
    "lint": "tslint gulpfile.ts gulp/**/*.ts",
    "postinstall": "typings install",
    "prebuild": "rm -rf dist",
    "test": "npm run lint && npm run build"
  },
  "license": "MIT",
  "devDependencies": {
    "@uncovertruth/tslint-config": "^0.0.1",
    "gulp": "^3.9.1",
    "gulp-connect": "^4.0.0",
    "gulp-if": "^2.0.1",
    "gulp-pug": "^3.0.3",
    "pug": "^2.0.0-beta2",
    "require-dir": "^0.3.0",
    "tslint": "^3.10.2",
    "typescript-require": "^0.2.9-1",
    "typings": "^1.0.4"
  }
}
